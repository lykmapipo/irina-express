'use strict';

//dependencies

/**
 * @function
 * @description implementation of irina express signup workflows
 * @public
 */
module.exports = {
    /**
     * @function
     *
     * @description handle and process Http POST /signup
     * @param  {HttpRequest} request
     * @param  {HttpResponse} response
     */
    post: function postSignup( /*User*/ ) {
        //return middleware
        return function( /*request, response*/ ) {

        };
    }
};